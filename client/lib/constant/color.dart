import 'package:flutter/material.dart';

/// カラー定義クラス
class ColorDefinitions {
  // プライマリカラー
  static const primaryColor = Color(0xFFF1F3F8);

  // セカンダリーカラー
  static const secondaryColor = Color(0xFFD6E0F0);

  // アクセントカラー
  static const accentColor = Color(0xFF8D93AB);

  // テキストカラー
  static const textColor = Color(0xFF393B44);

  // BoxShadowレイヤーカラー1
  static const boxShadowLayer1 = Color(0x26000000);

  // BoxShadowレイヤーカラー2
  static const boxShadowLayer2 = Color(0x4D000000);

  // 入力フォーム等のラベル文字色
  static const formLabelTextColor = Color(0xFFBDBDBD);
}
